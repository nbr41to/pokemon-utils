import type { FC } from 'react';

export const CompatibilityTable: FC = () => {
  // const columns = [];

  return (
    <div className="h-80 w-80 border">
      {/* {columns.map((column) => (
        <div>{column}</div>
      ))} */}
    </div>
  );
};
